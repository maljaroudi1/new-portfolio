import React from 'react';
import { motion } from 'framer-motion';
import { styles } from '../styles';
import { services } from '../constants';
import { fadeIn, textVariant } from '../utils/motion';
import { SectionWrapper } from '../hoc';
import '../styles/about/about.css'
const ServiceCard = ({ index, title, icon }) => {
  return (
    <motion.div
      variants={fadeIn('right', 'spring', 0.5 * index, 0.75)}
      className="xs:w-[250px] w-full card-gradient p-[1px] rounded-[20px] shadow-card">
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-jetLight rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col">
        <img src={icon} alt={title} className="w-16 h-16 object-contain" />
        <h3 className="text-taupe text-[18px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  );
};

const About = () => {
  return (
    <div className="-mt-[6rem]">
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn('', '', 0.1, 1)}
        className="mt-4 text-taupe text-[18px] max-w-3xl leading-[30px]">
        Welcome to my portfolio, a showcase of my journey as a Full Stack React.js Developer.
        My passion lies in creating immersive web experiences that seamlessly blend aesthetics and functionality.
        With my expertise in React.js, I specialize in designing and developing interactive front-end interfaces integrated with robust server-side solutions.
        My diverse skill set spans HTML, SCSS, ReactJS, Node.js, MongoDB, ExpressJS, NextJS and Firebase, enabling me to take projects from concept to deployment.
        These projects serve as a testament to my technical prowess and creativity, illustrating my ability to excel in both front-end and back-end development.
         Explore my portfolio to witness the depth of my capabilities and dedication to crafting remarkable web solutions
      </motion.p>

      <div className="box mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </div>
  );
};

export default SectionWrapper(About, 'about');
